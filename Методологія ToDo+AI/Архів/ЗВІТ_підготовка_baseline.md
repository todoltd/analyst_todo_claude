# Звіт: Підготовка + baseline (Задача #11)

**Статус:** ✅ виконано. Наступне — Задача #12 (`kickoff-agenda-finalizer`).

## Метод роботи (через недоступність джерела)
Вихідна тека зі скілами в цій сесії недоступна з пісочниці (mount down). Тому:
- працюю на **робочих копіях**, знятих з установленого плагіна **v0.1.4** (`plugin_018ciPGq3d27cKoFn4RPk6U2`, read-only) — це актуальне упаковане стану скілів;
- виправлені скіли + звіти складаю в **корінь проекту** (записуваний), мердж у вихідну теку — вручну (або після перепідключення теки правлю джерело напряму).

## Що зроблено
1. **Робоча копія** 10 скілів у пісочниці (writable).
2. **Реконструйовано валідатор** `валідатор_скілів.py` (вихідний у недоступній теці) — 7 інваріантів вмісту.
3. **Знайдено й виправлено 2 хибні спрацювання у самому валідаторі** (дисципліна «перевір перевіряльника»):
   - `[Назва компанії]` помилково вважався забороненим — насправді це **легітимний плейсхолдер** (заголовки документів, інструкції). Прибрано із заборонених.
   - латинський `kickoff` у **англомовних назвах** скілів (`# Kickoff Agenda Finalizer`) помилково підсвічувався — додано виняток для ідентифікаторів/назв.
   - детектор «зʼїдених імен» **навмисно не плутає** `` з потрійним ```code-fence``` (та сама пастка, що дала хибну впевненість у минулій сесії).
4. **Пристинна baseline-копія** `skills_baseline/` для диффів (git тут ненадійний — пісочниця блокує `unlink`; порівнюю через `diff -r skills skills_baseline`).

## Baseline (після виправлення валідатора)
```
10 скілів — 🔴 0 критичних, 🟡 0 попереджень. ✅ Усі інваріанти пройдені.
```
**Висновок:** упаковані скіли v0.1.4 **механічно чисті**. Ітерація 1 (механіка/консистентність) для всіх 7 цільових скілів — **зелена на старті**. Реальні ризики зосереджені в ітераціях 2–5 (структура → логіка/методологія → домен → рантайм).

## Цикл кожної ітерації (нагадування)
1. Аналіз ризиків і помилок — дуже ретельно (🔴/🟡/🟢).
2. Пропозиція з оптимізації.
3. Внесення правок (у робочу копію).
4. Перевірка, що все внесено — `валідатор_скілів.py` + `diff` проти baseline (по вмісту, не по наявності рядків).

## Інструменти й артефакти
- `валідатор_скілів.py` — обовʼязковий крок 4 кожної ітерації.
- `skills_baseline/` — еталон для диффів.
- `ШАБЛОН_звіту_ітерації.md` — однаковий формат звіту для всіх ітерацій.
